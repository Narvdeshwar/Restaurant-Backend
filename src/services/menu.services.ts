interface itemsProps {
    name: String,
    price: Number,
    description?: String,
    isAvailable?: Boolean
}[]
export const createMenu = async (title: string, items: itemsProps[], RestaurantId: string) => {

    return createMenu;
}